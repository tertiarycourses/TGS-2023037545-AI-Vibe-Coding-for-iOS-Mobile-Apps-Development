set(CMAKE_CXX_STANDARD 20)
if(CMAKE_CXX_COMPILER_ID MATCHES "Clang|GNU")
  target_compile_options(DomainCore PRIVATE -Wall -Wextra -Werror)
endif()
option(ENABLE_ASAN "Address sanitizer" OFF)
if(ENABLE_ASAN)
  target_compile_options(DomainCore PRIVATE -fsanitize=address)
  target_link_options(DomainCore PRIVATE -fsanitize=address)
endif()
