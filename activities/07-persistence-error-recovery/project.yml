name: Activity07Recovery
options:
  bundleIdPrefix: sg.tertiary.course
settings:
  base:
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    SWIFT_VERSION: "6.0"
targets:
  Activity07Recovery:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: solution/Sources
      - path: solution/Assets.xcassets
        buildPhase: resources
      - path: solution/PrivacyInfo.xcprivacy
        buildPhase: resources
    info:
      path: solution/Info.plist
      properties:
        CFBundleDisplayName: Activity07Recovery
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: sg.tertiary.course.activity07recovery
        MARKETING_VERSION: "1.2"
        CURRENT_PROJECT_VERSION: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: NO
  Activity07RecoveryTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    sources: [solution/AppTests]
    dependencies:
      - target: Activity07Recovery
schemes:
  Activity07Recovery:
    build:
      targets:
        Activity07Recovery: all
    test:
      targets:
        - name: Activity07RecoveryTests
